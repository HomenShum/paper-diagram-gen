const fs = require("fs"); const path = require("path"); const files = JSON.parse(fs.readFileSync(path.join(__dirname, "_files.json"), "utf8")); for (const [relPath, content] of Object.entries(files)) { const fullPath = path.join(__dirname, relPath); const dir = path.dirname(fullPath); if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true }); fs.writeFileSync(fullPath, content); console.log("Wrote", relPath, "(" + content.length + " chars)"); } console.log("All files written");